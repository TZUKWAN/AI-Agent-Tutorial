# 笔记：Agent 的六个零件

一个最小可运行的 Agent 通常由六个零件拼成：

1. System Prompt：岗位说明书，告诉它“你是谁、能做什么”。
2. Tools：它能调用的外部能力，例如读文件、计算器、查时间。
3. Context：当前对话历史，让它记得刚才聊过什么。
4. State：持久化状态，落到文件或数据库里，重启不丢。
5. Human Approval：危险操作前停下来等人确认。
6. Logging：每一步都写日志，出问题可追溯。

本项目 `agent.py` 把这六个零件各落了一处代码。
