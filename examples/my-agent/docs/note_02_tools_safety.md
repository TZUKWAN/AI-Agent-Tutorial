# 笔记：工具调用与安全边界

工具（Tool）是 Agent 与外部世界交互的手。每个工具应当：

- 输入输出结构化，例如统一返回 `{"ok": bool, "result": ..., "error": ...}`。
- 最小权限：读文件工具要限制在工作目录内，拒绝越界路径。
- 危险动作（删除、发送、覆盖）一律先请求人工批准。

本项目的 `calculator` 用 AST 白名单解析表达式，
`__import__('os').system(...)` 这类注入会被直接拒绝。
