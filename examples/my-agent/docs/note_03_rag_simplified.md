# 笔记：检索增强（RAG）的教学简化版

真正的 RAG 会把文档切成块、做向量嵌入、再按语义相似度召回。
本教学项目不依赖任何模型，因此用**关键词词频打分**做一个简化版：

1. 把 docs/ 目录下所有 .md 读进来。
2. 对查询和文档分别做最简单的分词（英文按词、中文按字与二元组）。
3. 用查询词在文档中出现的次数求和作为相关度分数。
4. 返回分数最高的前 N 篇。

它不是向量检索，不理解语义，只认字面命中——这是刻意的教学简化。
